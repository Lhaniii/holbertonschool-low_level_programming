#include "main.h"
/**
 * _islower - print x10 alphabet
 * Return: Always (succes)
 * @c: crÃation entiÃ©
  */
int _islower(int c)
{
		if (c >= 'a'  && c <= 'z')
			return (1);
		else
			return (0);
}
