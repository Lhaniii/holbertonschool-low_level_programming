#include<stdio.h>
/**
 * main - alphabet in mirror
 *
 * Return: Always (succes)
 */
int main(void)
{
	char tebahpla;

	for (tebahpla <= 'z' ; tebahpla  'a' ; tebahpla++)
		putchar(tebahpla);

	putchar('\n');

	return (0);
}
