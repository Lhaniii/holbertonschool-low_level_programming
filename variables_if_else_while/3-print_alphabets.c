#include<stdio.h>
/**
 * main - alphabet
 *
 * Return: Always (succes)
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a' ; alphabet <= 'z' ; alphabet++)
		putchar(letter);

	for (alphabet = 'A' ; alphabet <= 'z' ; alphabet++)
		putchar(letter);

	putchar('\n');

	return (0);
}
