#include<stdio.h>
/**
 * main - number - char
 *
 * Return: always (succes)
 */
int main(void)
{
	while (number <= 9)

		for (number = '0'; number <= '9'; alphabet++)
			putchar(number);

	putchar('\n')

	return (0);
}
